tunnel: <TUNNEL_UUID>
credentials-file: /root/.cloudflared/<TUNNEL_UUID>.json

ingress:
  # wildcard to caddy
  - hostname: "blavogiez.fr"
    service: http://<CADDY_LXC_IP>:80
  - hostname: "*.blavogiez.fr"
    service: http://<CADDY_LXC_IP>:80

  # sinon obligatoire
  - service: http_status:404
